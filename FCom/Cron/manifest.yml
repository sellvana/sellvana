modules:
    FCom_Cron:
        version: 0.1.1
        require: { module: { FCom_Core: 0.1.0 } }
        description: Cron scheduled tasks manager

        default_config:
            modules:
                FCom_Cron:
                    mode_by_ip: |
                        127.0.0.1:DEBUG
                        DISABLED

        areas:
            FCom_Admin:
                auto_use: [ layout, views ]

            FCom_Cron:
                routing:
                    - [ get, /, FCom_Cron_Controller.run ]
                    - [ get, /*task, FCom_Cron_Controller.run ]
