<div class="f-site-main" role="main">
  <div class="container">
    {{ THIS.view('core/messages') | raw }}
    <form action="{{ APP.href('customer/register') }}" method="post" id="register-form" role="form">
      <fieldset class="f-authentification-form f-register-form">
      <header class="f-page-header">
        <h1 class="f-page-title">{{ 'Register on Fulleron' |_ }}</h1>
      </header>
        <div class="row">
          <div class="form-group col-sm-6">
            <label for="register-firstname" class="control-label required">{{ "First Name" | _ }}</label>
            <div class="controls">
              <input type="text" name="model[firstname]" class="required form-control" id="register-firstname"/>
            </div>
          </div>
          <div class="form-group col-sm-6">
            <label for="register-lastname" class="control-label required">{{ "Last Name" | _ }}</label>
            <div class="controls">
              <input type="text" name="model[lastname]" class="required form-control" id="register-lastname"/>
            </div>
          </div>
        </div>
        <div class="form-group">
          <label for="register-street1" class="control-label required">{{ "Address" | _ }}</label>
          <div class="controls">
            <input type="text" name="address[street1]" class="required form-control" id="register-street1"/>
          </div>
        </div>
        <div class="row">
          <div class="form-group col-sm-6">
            <label for="register-email" class="control-label required">{{ "Email" | _ }}</label>
            <div class="controls">
              <input type="email" name="model[email]" class="required form-control" id="register-email"/>
            </div>
          </div>
        </div>
        <div class="row">
          <div class="form-group col-sm-6">
            <label for="model-password" class="control-label required">{{ "Password" | _ }}</label>
            <div class="controls">
              <input type="password" name="model[password]" class="required form-control" id="model-password"/>
            </div>
          </div>
          <div class="form-group col-sm-6">
            <label for="register-password_confirm" class="control-label required">{{ "Confirm Password" | _ }}</label>
            <div class="controls">
              <input type="password" name="model[password_confirm]" class="required form-control" equalto="#model-password" id="register-password_confirm"/>
            </div>
          </div>
        </div>
        <div class="form-group">
          <input type="submit" class="btn btn-primary btn-lg" value="{{ "Register" | _ }}"/>
          <span class="help-block f-required-notice">* {{ "Indicates Required Fields" | _ }}</span>
        </div>
      </fieldset>
    </form>
  </div>
</div>
<script>
require(['jquery', 'jquery.validate'], function($) {
    $(function() {
        $('#register-form').validate();
    })
})
</script>
