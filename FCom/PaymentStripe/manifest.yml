modules:
    FCom_PaymentStripe:
        version: 0.1.0
        require: { module: { FCom_Sales: 0.3.0 } }
        auto_use: [ bootstrap ]

        areas:
            FCom_Admin:
                auto_use: [ layout, views ]

            FCom_Frontend:
                auto_use: [ layout, views ]
                routing:
                    - [ /stripe, FCom_PaymentStripe_Frontend_Controller ]

        default_config:
            api_url: "https://api.stripe.com"
            test: true
            test_secret_key: