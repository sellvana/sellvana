modules:
    FCom_FreshBooks:
        version: 0.1.0
        channel: beta
        require: { module: { FCom_Core: 0.1.0 } }
        description: "FreshBooks&reg; payment method and invoice management API integration"
        observe:
            - [ "FCom_Sales_Model_Order::invoice", FCom_FreshBooks.createInvoiceFromOrder ]

        areas:
            FCom_Admin:
                auto_use: views
