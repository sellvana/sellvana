base:
    - view: head
        do:
            - [ requireJs, fcom.pushclient, "@FCom_PushServer/js/fcom.pushclient" ]

    - { callback: "FCom_PushServer_Main::layoutInit" }
/settings:
    - view: settings
        do:
            - [ addTabGroup, pushserver, { label: Push Server } ]
            - [ addTab, FCom_PushServer, { label: Push Server, group: pushserver, async: true } ]
