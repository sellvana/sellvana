{% set prod = THIS.get('product') %}
{% set shopper = prod.getData('frontend_fields') %}
{% if shopper %}
    <div id="f-prod-tab-shopper">
        <div class="f-tab-heading">
            <h5 class="f-tab-title">{{ 'Shopper'|_ }}</h5>
        </div>
        {% for sh in shopper %}
            <div class="form-group" style="overflow: hidden">
                <label class="col-md-2 control-label">{{ sh.label|_ }}</label>
                <div class="col-md-3">
                    {% if sh.input_type == 'select' %}
                        {% set options = sh.options|split(',') %}
                        <select name="shopper[{{ sh.field_code }}]" class="form-control">
                            {% for op in options %}
                                <option value="{{ op }}">{{ op|_ }}</option>
                            {% endfor %}
                        </select>
                    {% elseif  sh.input_type == 'textarea' %}
                        <textarea name="shopper[{{ sh.field_code }}]" class="form-control" cols="3" rows="2"></textarea>
                    {% elseif sh.input_type == 'text' %}
                        <input type="text" name="shopper[{{ sh.field_code }}]" class="form-control"/>
                    {% endif %}
                </div>
                <label class="col-md-3 control-label">{{ "Price: "~ sh.price|currency |_ }}</label>
            </div>
        {% endfor %}
    </div>
{% endif %}