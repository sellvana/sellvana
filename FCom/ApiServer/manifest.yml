modules:
    FCom_ApiServer:
        version: 0.1.0
        channel: beta
        require: { module: { FCom_Core: 0.1.0 } }
        description: "API Server area"
        auto_use: [ bootstrap ]

        areas:
            FCom_Admin:
                auto_use: [ views, layout ]
