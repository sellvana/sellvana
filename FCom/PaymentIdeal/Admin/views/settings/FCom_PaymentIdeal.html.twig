{% set c = THIS.get('model') %}
{% set fieldData = { model: c, settings_module: 'FCom_PaymentIdeal' } %}
{% import THIS.view('admin/form-elements').twigName() as forms %}
{{ forms.accordion_start({id:'settings-FCom_PaymentIdeal'}) }}
     {{ forms.accordion_panel_start({label:'iDEAL Settings'|_, id:'settings-FCom_PaymentIdeal-group0', in:1}) }}
        {{ forms.boolean(fieldData|merge({field:'active', label:'Enabled'|_ })) }}
        {{ forms.input(fieldData|merge({field:'title', label:'Display Title'|_ })) }}
        {{ forms.input(fieldData|merge({field:'partner_id', label:'Pertner ID'|_ })) }}
        {{ forms.input(fieldData|merge({field:'profile_key', label:'Profile Key'|_ })) }}
        {{ forms.input(fieldData|merge({field:'sort_order', label:'Payment Method Sort Order'|_ })) }}
        {{ forms.select(fieldData|merge({field:'order_status', label:'New Order Status'|_, options: {'':'Select One', 'processing':'Processing'} })) }}
        {{ forms.input(fieldData|merge({field:'recent_day', label:'Orders Recent Days'|_, attr: 'data-rule-number="true"' })) }}
        {{ forms.boolean(fieldData|merge({field:'test', label:'Test Mode'|_ })) }}
    {{ forms.accordion_panel_end }}
{{ forms.accordion_end }}

