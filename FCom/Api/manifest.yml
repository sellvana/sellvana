modules:
    FCom_Api:
        version: 0.1.0
        require: { module: { FCom_Core: 0.1.0 } }
        description: "API area"
        auto_use: [ bootstrap ]

        areas:
            FCom_Admin:
                auto_use: [ views ]