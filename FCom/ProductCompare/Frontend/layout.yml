base:
    - view: head
        do:
            - [ requireJs, fcom.productcompare, "@FCom_ProductCompare/Frontend/js/fcom.productcompare" ]
            - [ requireRun, fcom.productcompare ]

/catalog/compare:
    - { include: base }
    - { hook: main, views: catalog/compare }

/catalog/compare/xhr:
    - { root: catalog/compare }
