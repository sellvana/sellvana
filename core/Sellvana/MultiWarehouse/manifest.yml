modules:
    Sellvana_MultiWarehouse:
        version: 0.1.0
        channel: alpha
        require: { module: { FCom_Core: 0.1.0 } }

        areas:
            FCom_Admin:
                auto_use: [ views, layout ]
