modules:
    Sellvana_PaymentBasic:
        version: 0.1.0
        channel: alpha
        translations: { en: en.php }
        description: "Basic payment methods (check/money order, purchase order)"
        require: { module: { Sellvana_Sales: 0.1.10 } }
        auto_use: [ bootstrap ]

        areas:
            FCom_Admin:
                auto_use: [ views, layout ]

            FCom_Frontend:
                auto_use: [ views ]
