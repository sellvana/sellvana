modules:
    Sellvana_CustomerChat:
        version: 0.1.0
        require: { module: { FCom_Core: 0.1.0, FCom_PushServer: 0.1.0 } }

        #todo:
            # push link to user
            # shadow browsing (share session and cart)
