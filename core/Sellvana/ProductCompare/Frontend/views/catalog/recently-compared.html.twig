{% set cnt = THIS.get('cnt') ?: 6 %}
{% set products = APP.instance('Sellvana_ProductCompare_Model_History').getVisitorProducts(cnt) %}

{% if products %}
    <div class="f-widget f-widget-featured-prods f-prod-listing f-prod-listing-grid f-widget-recently-compared-prods">
        <header class="f-widget-header">
            <strong class="f-widget-title">{{ 'Recently Compared Products'|_ }}</strong>
        </header>
        {{ THIS.view('catalog/product/grid-tiles').set({
            lg_columns: cnt,
            md_columns: cnt,
            sm_columns: 4,
            xs_columns: 2,
            products: products
        }) | raw }}
    </div><!-- /.f-widget-recently-compared-prods -->
    <div class="clearfix"></div>

{% endif %}